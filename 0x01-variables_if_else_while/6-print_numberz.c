#include <stdio.h>

/**
 * main - entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	int limit;

	limit = '9';

	for (i = '0', i <= limit; i++)
		putchar(d);
	putchar('\n');
	return (0);
}
